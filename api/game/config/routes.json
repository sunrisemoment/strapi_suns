{
    "routes": [
        {
            "method": "GET",
            "path": "/game/:id",
            "handler": "game.getGame",
            "config": {
                "policies": []
            }
        }
    ]
}
