{
    "routes": [
        {
            "method": "GET",
            "path": "/stteams",
            "handler": "stteamsapi.teams",
            "config": {
                "policies": []
            }
        }
    ]
}
